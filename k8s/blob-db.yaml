apiVersion: v1
kind: PersistentVolume
metadata:
  name: blob-db-pv
  labels:
    type: local
spec:
  storageClassName: standard
  capacity:
    storage: 250Mi
  accessModes:
    - ReadWriteOnce
  hostPath:
    path: "/mnt/data/blob-db"
  persistentVolumeReclaimPolicy: Retain

---

apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: blob-db-pv-claim
  labels:
    app: blob-db
spec:
  storageClassName: standard
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 250Mi

---

apiVersion: v1
kind: Service
metadata:
  name: blob-db-service
  labels:
    app: blob-db
spec:
  ports:
    - name: user-db-port
      port: 5433
      protocol: TCP
      targetPort: 5432
  selector:
    app: blob-db
    tier: blob-db
  clusterIP: None

---

apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: blob-db-stateful-set
  labels:
    app: blob-db
spec:
  serviceName: "blob-db-service"
  replicas: 1
  selector:
    matchLabels:
      app: blob-db
      tier: blob-db
  template:
    metadata:
      labels:
        app: blob-db
        tier: blob-db
    spec:
      containers:
        - name: postgres
          image: postgres:16.2
          ports:
            - containerPort: 5432
              name: blob-db-port
          env:
            - name: PGUSER
              valueFrom:
                secretKeyRef:
                  name: secrets
                  key: blob-db-pguser
            - name: POSTGRES_DB
              valueFrom:
                secretKeyRef:
                  name: secrets
                  key: blob-db-postgres-db
            - name: POSTGRES_USER
              valueFrom:
                secretKeyRef:
                  name: secrets
                  key: blob-db-postgres-user
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: secrets
                  key: blob-db-postgres-password
          volumeMounts:
            - name: blob-db-persistent-storage
              mountPath: /var/lib/postgresql/blob-data
  volumeClaimTemplates:
    - metadata:
        name: blob-db-persistent-storage
      spec:
        storageClassName: standard
        accessModes:
          - ReadWriteOnce
        resources:
          requests:
            storage: 250Mi
